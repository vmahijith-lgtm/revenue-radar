# Copy this to .env and fill in your values
# Attribution Engine — Environment Variables

# API host/port (optional — defaults work locally)
API_HOST=0.0.0.0
API_PORT=8000

# Streamlit port (optional)
STREAMLIT_PORT=8501

# dbt profile (default: dev)
DBT_TARGET=dev
